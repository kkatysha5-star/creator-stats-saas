{
  "name": "creator-stats",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node backend/server.js",
    "build": "cd frontend && npm ci --prefer-offline && npm run build && cd ../backend && npm ci --prefer-offline"
  }
}
